specVersion: "v1"
serviceId: "services"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "services-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./example"]
